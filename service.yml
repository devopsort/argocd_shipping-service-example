apiVersion: v1
kind: Service
metadata:
  name: ms-shipping-svc
  namespace: default
spec:
  type: NodePort
  selector:
    app: ms-shipping
  ports:
  - protocol: TCP
    port: 30003
    targetPort: 8080
    name: default