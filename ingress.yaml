apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ingress-msship
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - http:
      paths:
      - path: /ms-shipping
        backend:
          serviceName: ms-shipping-svc
          servicePort: 30003
